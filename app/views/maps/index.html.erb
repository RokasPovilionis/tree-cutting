
<!DOCTYPE html>
<html>
  <head>
  </head>
  <body>
  <div class="container-fluid">
    <div class="row justify-content-center">

      <div class="col-sm-4">
        <h2 class="text-center">Please select wanted permits</h2>
        <%= bootstrap_form_with url: '/maps', remote: true do |form| %>
          <%= form.select :uredija, uredijos %>
          <%= form.select :kirtimo_rusis, kirtimo_tipai %>
          <%= form.select :nuosavybes_forma, nuosavybes_formos %>
          <%= form.submit %>
        <% end %>
      </div>
    </div>
  </div>
  </body>
</html>
